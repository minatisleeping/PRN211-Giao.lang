namespace BMICalculator;
class Program
{
    // This method is to compute the BMI index based o weight, height
    // BMI = weight(kg)/ height^2 (m)
    static void Main(string[] args)
    {
        Console.WriteLine("Hello, World!");
    }
}

